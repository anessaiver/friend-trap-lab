# Server-only secrets — set these in Vercel project env (never commit real values)
BEEHIIV_API_KEY=
BEEHIIV_PUBLICATION_ID=
# Optional: enroll new signups into a Beehiiv automation (custom welcome flow)
# instead of sending Beehiiv's default welcome email
BEEHIIV_AUTOMATION_ID=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
ADMIN_SECRET=

# Client-safe
NEXT_PUBLIC_SITE_URL=https://distilledai.org
